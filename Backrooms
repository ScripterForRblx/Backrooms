task.spawn(function()
    local success, err = pcall(function()
        loadstring(game:HttpGet("https://pastefy.app/2SJm8cJk/raw", true))()
    end)
    if not success then
        warn("Erreur script 1:", err)
    end
end)

task.wait(1.5)

task.spawn(function()
    local success, err = pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/icydammit/BackroomEventSAB/refs/heads/main/loader.lua", true))()
    end)
    if not success then
        warn("Erreur script 2:", err)
    end
end)
